硬盘格式化：取消挂载，删除，新建分区，删除；

刷镜像：RUFUS；

重启，进入boot，security，security boot -》disabled（从硬盘读取开机项，而不从内部硬盘读取）

重启，U盘启动设备放到最前

重启，进入Ubuntu，

中文，安装，继续，不要连wifi（防止升级系统包，后面再升级），最小安装，先不装驱动，其他）

dev 设备，nvme固态，

dev/sda 空闲，添加 1024MB，逻辑分区，起始位置，用于EF1系统分区（开机检查前50g）

dev/sda5 空闲（16384MB）逻辑，起始，交换空间（神经网络需要更大）（跟内存一样大）（16G）

dev/ sda6 空闲61440MB，主分区，起始，Ext4，/（根分区）(60G)

dev/sda2 空闲，318856MB，主分区，起始， /home（重装只需要重装根分区） （home分区，不需要重装）（除了根分区与opt外）（311G）

dev/sda3 空闲，102373MB，主分区 Ext4 /opt （100G）

(不能跳过，要看提示框）

修复

（如删掉usr/local)（根损坏，home与opt安全）（usr隶属于根，不隶属于home）
强制关机

插U盘，boot提高，

找到原根分区，ext4，大小不变，格式化根分区，挂载/

三个主分区，home与opt重新挂载到原来位置，

（熟练的话，可以将usr/local单独挂出）

公用软件opt

home文件只有自己访问